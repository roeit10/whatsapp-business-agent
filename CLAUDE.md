# CLAUDE.md

סוכן AI שעונה ללקוחות בוואטסאפ במקום בעל העסק. Node אפס תלויות, רץ על Railway.

## ⚡ הדבר הראשון בכל סשן

**הרץ `npm run doctor` לפני שאתה עונה על שאלה כלשהי או נוגע בקוד.**

הבדיקה מסתיימת בשורת `NEXT:`:

- `NEXT: READY` — הכל מחובר. אפשר לעבוד רגיל. **להעלאה לאוויר — לפי סעיף `DEPLOY` ב-`ONBOARDING.md`, כולל חיבור הריפו לשירות ו-Volume. שני אלה נראים אופציונליים ואינם.**
- `NEXT: STEP N` — **המשתמש באמצע הקמה. פתח את `ONBOARDING.md` ובצע את השלב הזה, אותו בלבד.**

אל תדלג על זה גם אם המשתמש פותח בשאלה אחרת. משתמש שמבקש "תוסיף לי כלי" על התקנה חצי גמורה יקבל שגיאות שנראות כמו באגים בקוד ואינן.

### חוקי האונבורדינג

1. **שלב אחד בכל פעם.** לא לפתוח שלב חדש לפני שהקודם אומת.
2. **אימות = הרצת `npm run doctor`.** "התחברתי" מהמשתמש הוא סימן להריץ בדיקה, לא לסמן וי.
3. **בקשה אחת בכל הודעה.** לא רשימת מטלות במקביל.
4. **לפני התקנה — לבקש אישור** ולהסביר מה מותקן ולמה.
5. **לא להמציא ערכים.** מפתח או מזהה שלא נמסר — מבקשים.
6. **מה שדורש זהות — המשתמש עושה.** התחברות בדפדפן, סריקת QR, פתיחת חשבון. לתת קישור ולחכות.

## מבנה

| קובץ | תפקיד |
|---|---|
| `src/index.js` | שרת HTTP: webhook של Green API + עמודי האתר |
| `src/agent.js` | פרומפט המערכת ולולאת הכלים מול OpenRouter |
| `src/tools.js` | הגדרות הכלים והקוד שמריץ אותם |
| `src/data/google.js` | משיכת הדוק והגיליונות + פרסור CSV |
| `src/site.js` | רינדור עמודי הקטלוג מהגיליון |
| `src/memory.js` | היסטוריית שיחה פר לקוח |
| `src/knowledge.js` | תשובות שבעל העסק מסר בקבוצה, נספחות למאגר הידע |
| `src/tickets.js` | שאלות שהועברו לבעל העסק וממתינות לתשובה |
| `src/text.js` | נרמול והשוואת מחרוזות, משותף לחיפוש ולמאגר הנלמד |
| `src/green-api.js` | שליחת הודעות, יצירת קבוצה והגדרת webhook |
| `scripts/create-group.js` | יצירת קבוצת השאלות הפנימית |
| `scripts/doctor.mjs` | בדיקת מוכנות |

## החלטות שאסור לשבור

**קריאה בלבד, בלי הזדהות.** הנתונים נמשכים מנתיב ה-`export` של גוגל, שעובד על קובץ משותף בלי טוקן. ה-API הרשמי מחזיר 401 גם לקובץ ציבורי. אל תחליף לספרייה שדורשת הרשאות בלי שהמשתמש ביקש כתיבה במפורש.

**מספר השולח נסגר ב-closure ולא עובר כפרמטר של כלי.** אם הוא יהיה פרמטר, אפשר לשכנע את המודל בשיחה למשוך הזמנה של לקוח אחר.

**כמות המלאי לא מגיעה למודל.** `publicProduct` מסירה את העמודה ומחליפה ב"במלאי"/"אזל". הוראה בפרומפט נשברת בדיוק כששואלים "כמה נשאר לכם" — נתון שלא סופק לא יכול לדלוף.

**"לא נמצא" ו"אזל" הן תשובות שונות.** אסור לערבב. מוצר שלא בגיליון אינו מוצר שאזל.

**חיפוש עמיד לשגיאות כתיב.** דמיון בייגרמים ומרחק עריכה, סף 0.58, מינימום 4 תווים. לקוחות מקלידים שמות מותגים בשגיאה כל הזמן.

**כשהכל אזל — מחזירים חלופות מדורגות לפי "מתאים ל", לא לפי מילות החיפוש.** דירוג לפי הטקסט החזיר חמישה מזונות לחתול בוגר למי שביקש מזון לגור, רק כי חלקו מותג.

**"לא יודע" הוא כלי שהמודל קורא לו, לא זיהוי בדיעבד של הטקסט.** `ask_owner` מעביר את השאלה לקבוצה הפנימית. ניסיון לזהות "אין לי מידע" מתוך התשובה שכבר נוסחה תופס גם "המוצר אזל" ומפספס תשובות מומצאות — בדיוק הפוך ממה שצריך.

**מה שנלמד מבעל העסק נשמר כברירת מחדל ב-`data/learned.json`, לא בדוק של גוגל.** הדוק נקרא דרך נתיב ה-`export` בלי הזדהות, ולכן הוא קריאה בלבד. הקובץ נספח לתשובת `get_shop_info` אחרי הדוק, כך שבעל העסק ממשיך לערוך את הדוק ביד ושום דבר לא נדרס.

**לפני שמחברים את הפיצ'ר — לשאול את המשתמש מאיפה נקרא מאגר הידע ולאן נכתב מה שנלמד.** Google Docs ו-Sheets הם ברירת המחדל של התבנית, לא הנחה על העסק. יש מי שיחבר Notion, Airtable, CRM או בסיס נתונים — ואם המקור שלו ניתן לכתיבה, עדיף לכתוב אליו ולא לקובץ מקומי שיושב בצד. `src/knowledge.js` הוא נקודת ההחלפה היחידה: `addFact` שומר עובדה אחת, `knowledgeAddendum` מחזיר את הטקסט שנספח לתוצאת `get_shop_info`. מחליפים את שתיהן ושאר הקוד לא משתנה.

**מכל הקבוצות בוואטסאפ, רק `OWNER_GROUP_CHAT_ID` נענית, והיא לעולם לא מקבלת תשובת סוכן ללקוח.** בוט שעונה בקבוצות זו הדרך המהירה למספר חסום.

**הודעה בקבוצה שאינה תשובה ("רגע, בודק") לא נשלחת ללקוח.** המודל מסווג `answered` לפני שמשהו יוצא, והשאלה נשארת פתוחה. בלי זה אי אפשר לנהל שיחה רגילה בקבוצה.

**התשובה ללקוח והעובדה שנשמרת הן שני דברים נפרדים.** "תן לו 10% הנחה" היא החלטה נקודתית ואסור לה להיכנס למאגר הידע כמדיניות.

**תשובה שנגמרת ב"אין" בלי המשך היא תשובה כושלת.** תמיד חלופה, בירור, הצעה לעדכן, או טלפון של בעל העסק.

## סגנון

- כל טקסט שפונה למשתמש — עברית.
- הערות בקוד — אנגלית, ורק כשהן מסבירות **למה**, לא **מה**.
- אפס תלויות. אל תוסיף חבילה בלי לשאול.
- לפני כל שינוי בפרודקשן (deploy, שינוי webhook) — לומר מה עומד לקרות.
