# פרוטוקול אונבורדינג

מסמך זה מיועד ל-Claude Code, לא למשתמש. הוא מגדיר את סדר ההקמה.

## חוקי הפרוטוקול

1. **שלב אחד בכל פעם.** אין לפתוח שלב חדש לפני שהקודם אומת.
2. **אימות = `npm run doctor`, לא הצהרה של המשתמש.** משתמש שאומר "התחברתי" הוא סימן להריץ בדיקה, לא לסמן וי.
3. **אין לנחש ואין להמציא ערכים.** מפתח, מזהה או טוקן שלא נמסר במפורש — מבקשים.
4. **בקשה אחת בכל הודעה.** לא לשלוח למשתמש רשימה של חמישה דברים לעשות במקביל.
5. **לפני התקנה — לבקש אישור.** לומר מה מותקן ולמה, ולחכות לאישור.
6. **לא לגעת בקוד עד שהבדיקה מחזירה `NEXT: READY`.**

בכל שלב: להריץ `npm run doctor`, לקרוא את שורת ה-`NEXT`, ולעבוד על השלב שהיא מציינת.

---

## STEP 0 · ריפו משלכם

הריפו `roeit10/whatsapp-business-agent` הוא **תבנית לימוד**. אסור לעבוד בתוכו:

- Railway לא יכול לפרוס מריפו שהמשתמש אינו הבעלים שלו
- כל commit ינסה להידחף לחומר הקורס
- ההתאמות של המשתמש יתערבבו בחומר שכולם מקבלים

צריך ריפו עצמאי. אחרי שהמשתמש מחובר לגיטהאב (STEP 2), אפשר ליצור אותו:

```bash
gh repo create <שם-העסק>-agent --template roeit10/whatsapp-business-agent --private --clone
```

`--template` יוצר ריפו **עצמאי עם היסטוריה נקייה**, ולא fork. זה חשוב: fork של ריפו ציבורי הוא תמיד ציבורי, ומשתמש שיטעה ויעשה commit ל-`.env` יחשוף את המפתחות שלו לעולם.

אם גיטהאב עדיין לא מחובר — לבצע קודם את STEP 1 ו-STEP 2, ואז לחזור לכאן.

לעבור לתיקייה החדשה, להריץ `npm run doctor`, ולוודא שהוא מזהה ריפו משלכם.

---

## STEP 1 · כלי שורת פקודה

חסר `railway` או `gh`.

לומר למשתמש מה חסר ולמה צריך את זה — Railway כדי להעלות את הסוכן לשרת, GitHub כדי לנהל את הקוד. לבקש אישור להתקין, ואז להתקין:

```bash
# Railway
curl -fsSL agents.railway.com | sh          # macOS · Linux · WSL
brew install railway                        # חלופה למק

# GitHub
brew install gh                             # מק
winget install --id GitHub.cli              # Windows
```

להריץ `npm run doctor` ולוודא שהם מזוהים לפני המעבר הלאה.

---

## STEP 2 · התחברות בדפדפן

הכלים מותקנים אבל לא מחוברים. **את השלב הזה Claude לא יכול לעשות במקום המשתמש** — הוא דורש הזדהות אישית.

**קודם לוודא שיש חשבון Railway.** למי שאין, לשלוח את הקישור הזה ולא את הקישור הרגיל:

> **https://railway.com?referralCode=qSz7aA**
> נרשמים דרכו ומקבלים **20 דולר קרדיט מתנה** — מספיק לחודשים של הרצת הסוכן.

למי שכבר יש חשבון — לדלג ולהמשיך להתחברות. ל-GitHub נרשמים ב-https://github.com/signup.

ואז להריץ את הפקודה, ולומר למשתמש שייפתח דפדפן ושעליו לאשר:

```bash
railway login     # ייפתח דפדפן. אם אין דפדפן: railway login --browserless
gh auth login     # לבחור GitHub.com → HTTPS → Login with a web browser
```

לחכות שהמשתמש יאמר שסיים, ואז להריץ `npm run doctor` כדי לאמת. אם הבדיקה עדיין מראה "לא מחובר" — לא להמשיך, לבדוק מה קרה.

> **אם המשתמש התנתק בעבר מ-Railway:** `railway logout` מוחק גם את **קישורי הפרויקטים**, לא רק את ההזדהות. אחרי התחברות מחדש, פקודות כמו `railway up` יישאלו מחדש לאיזה פרויקט לפרוס או ייכשלו. הפתרון: `railway link` בתיקייה. זו תקלה שנראית כמו באג ואינה.

---

## STEP 3 · Green API (החיבור לוואטסאפ)

צריך שלושה דברים מהמשתמש, ורק הוא יכול לספק אותם.

לשלוח לו את הקישור ולהסביר בקצרה:

> צריך חשבון ב-https://green-api.com — הרשמה חינם.
> אחרי ההרשמה: יוצרים אינסטנס, ובמסך שלו סורקים QR **ממספר וואטסאפ ייעודי**.
>
> ⚠️ לא המספר העסקי הראשי. זה חיבור לא רשמי לוואטסאפ ומספר עלול להיחסם.
>
> כשזה מוכן, תשלח לי את `idInstance` ואת `apiTokenInstance` מהקונסולה.

כשהערכים מגיעים — לכתוב אותם ל-`.env` ולהריץ `npm run doctor`.

אם הבדיקה מחזירה מצב שאינו `authorized`: המצב `starting` הוא זמני, כדאי לחכות דקה ולבדוק שוב. אם זה נמשך — לבקש מהמשתמש לסרוק QR מחדש או ללחוץ "אתחול" בקונסולה.

---

## STEP 4 · OpenRouter (המוח)

> צריך מפתח מ-https://openrouter.ai/keys
> אחרי ההרשמה יוצרים מפתח ומטעינים קצת קרדיט. שיחה בהדגמה עולה סנטים בודדים.

לכתוב ל-`.env` ולהריץ `npm run doctor` — הבדיקה מוודאת שהמפתח תקין ומראה כמה קרדיט נשאר.

---

## STEP 5 · הנתונים בגוגל

צריך שלושה קבצים: מסמך ידע, גיליון מלאי, גיליון הזמנות.

**קודם לשאול:** "יש לך כבר את שלושת הקבצים בגוגל, או שנבנה אותם מאפס?"

### אם יש לו קבצים

לבקש את הקישורים, לחלץ את המזהה מכל כתובת (`/d/<ID>/`) ולכתוב ל-`.env`.
לקרוא את הכותרות בפועל ולהתאים את שמות העמודות בקוד אם הן שונות מברירת המחדל.

### אם אין לו קבצים

**לבדוק מה זמין לי כרגע:** האם יש בסשן הזה כלי שיודע ליצור קובצי Google Docs ו-Sheets.

- **אם יש** — להציע: "יש לי גישה ליצור קבצים בגוגל. רוצה שאבנה לך את שלושת הקבצים עם מבנה העמודות הנכון ודוגמאות?" ואם המשתמש מאשר, ליצור, לשתף ב"כל מי שיש לו את הקישור — צפייה", ולכתוב את המזהים ל-`.env`.

- **אם אין** — **לא להציע להקים חיבור כזה עכשיו.** זה מסיט מהמטרה ולוקח זמן. במקום זה:

  > אין לי כרגע גישה ליצור קבצים בגוגל, אז ניצור אותם ידנית — זה לוקח חמש דקות ואשלח לך בדיוק מה לשים בכל עמודה.
  >
  > (ואם בעתיד תרצה שאוכל ליצור ולערוך קובצי גוגל בעצמי, הדרך הפשוטה היא **Composio** — שירות שמחבר את קלוד לגוגל ולעוד מאות שירותים. לא נתעסק בזה עכשיו.)

  ואז להכתיב את המבנה:

  | קובץ | עמודות |
  |---|---|
  | מסמך ידע | טקסט חופשי: שעות, משלוחים, החזרות, מדיניות |
  | גיליון מלאי | `מקט · שם המוצר · מותג · קטגוריה · מתאים ל · גודל · מחיר · כמות במלאי · ימי אספקה` |
  | גיליון הזמנות | `מספר הזמנה · שם לקוח · טלפון · תאריך הזמנה · פריטים · סטטוס · תאריך משלוח צפוי · מספר מעקב` |

**בסוף השלב, לוודא שיתוף.** הבדיקה תיכשל על קובץ שלא משותף — גוגל מחזיר דף התחברות במקום את התוכן. זו התקלה הכי נפוצה בשלב הזה.

---

## STEP 6 · פרטי העסק

לשאול ולכתוב ל-`.env`: שם העסק, שם בעל העסק, טלפון להפניה.

בזמן הפיתוח מומלץ למלא `ALLOWED_SENDERS` במספר של המשתמש בלבד, כדי שהבוט לא יענה לאף אחד אחר. להסביר שמרוקנים את זה כשעולים לאוויר.

---

## STEP 7 · קבוצת השאלות (אופציונלי, אבל שווה)

הפיצ'ר שבו הסוכן מעביר לבעל העסק שאלה שאין לו עליה תשובה, ומה שנענה חוזר ללקוח ונשמר. **בקשה אחת בכל הודעה, כמו בכל שלב.**

1. לשאול את המשתמש אם הוא רוצה את זה. `OWNER_GROUP_CHAT_ID` ריק הוא מצב תקין — הסוכן פשוט יפנה לטלפון.
2. לבקש את מספר הוואטסאפ שלו בפורמט בינלאומי, ולומר שעומדת להיווצר קבוצה אמיתית שהוא יצורף אליה:
   ```bash
   npm run create-group -- 972501234567
   ```
3. לכתוב את `OWNER_GROUP_CHAT_ID` שהודפס ל-`.env`, ואחרי ה-DEPLOY גם למשתני הסביבה ב-Railway.

**ולשאול לפני הכל: איפה מאגר הידע שלו יושב, ולאן נכון שהתשובות שנלמדו ייכתבו.** ברירת המחדל היא קובץ מקומי, `data/learned.json`, כי מסמך גוגל משותף הוא קריאה בלבד. אבל יש מי שמחזיק את המידע ב-Notion, ב-Airtable, ב-CRM או בבסיס נתונים — ואם המקור שלו ניתן לכתיבה, עדיף לכתוב אליו ישירות במקום לקובץ נפרד שהוא לא רואה. אל תניח Google Docs רק כי זו ברירת המחדל של התבנית. ההחלפה היא שתי פונקציות ב-`src/knowledge.js` ותו לא.

---

## READY · אחרי שהבדיקה נקייה

רק עכשיו מתחילים לבנות. קודם לוודא שהמוח עונה מקומית:

```bash
npm run ask -- "יש רויאל קנין 2 קילו? כמה עולה?"
```

תשובה שקוראת לכלי ומחזירה נתון אמיתי = אפשר לעלות לאוויר.

---

## DEPLOY · העלאה ל-Railway

לומר למשתמש מה עומד לקרות לפני כל שלב שנוגע בשרת או בוואטסאפ.

```bash
railway init --name <שם-הפרויקט>          # 1. פרויקט
railway add --service <שם-השירות>          # 2. שירות
railway variables --set "KEY=VALUE" ...    # 3. כל מה שב-.env חוץ מ-PORT
railway up --detach                        # 4. בנייה והרצה — כאן מתחיל החיוב
railway domain                             # 5. כתובת ציבורית
```

### 6 · חיבור הריפו לשירות — **אסור לדלג**

`railway add --service` יוצר **Empty Service**, ו-`railway up` רק מעלה את תוכן התיקייה. בלי השלב הזה `git push` לא יפרוס כלום, וכל עדכון עתידי ידרוש `railway up` ידני. זו התקלה שמתגלה שבוע אחרי ההקמה, כשמישהו משנה קוד ולא מבין למה כלום לא קרה.

**קודם לוודא שהקוד בגיטהאב:**

```bash
git push origin main
```

**ואז לחבר.** הריפו של המשתמש פרטי, ולכן החיבור דורש ש-GitHub App של Railway יקבל גישה אליו — זהו **אישור בדפדפן שרק המשתמש יכול לתת**, בדיוק כמו STEP 2. שני מסלולים:

```bash
railway service source connect --repo <owner>/<repo> --branch main
```

אם הפקודה נכשלת על הרשאות, או אם המשתמש מעדיף לראות מה קורה — **המסלול בממשק**:

> `railway open` → בדשבורד ללחוץ על השירות → לשונית **Settings** → **Source** → **Connect Repo** → לבחור את הריפו.
> אם הריפו לא מופיע ברשימה: **Configure GitHub App** → לתת ל-Railway גישה לריפו הזה → לחזור ולבחור אותו.

אחרי החיבור לאמת שזה נתפס:

```bash
railway service status
```

מכאן כל `git push origin main` פורס אוטומטית, ואין יותר צורך ב-`railway up`.

### 7 · Volume — שמירת היסטוריית השיחות

`MEMORY_FILE` יושב על מערכת הקבצים של הקונטיינר, ו-Railway מוחק אותה **בכל דפלוי ובכל restart**. בלי ווליום הסוכן זוכר תוך כדי שיחה אבל מתחיל מאפס מול כל לקוח אחרי כל עדכון.

באותה תיקייה יושבים גם `LEARNED_FILE` ו-`TICKETS_FILE` — מה שהסוכן למד מבעל העסק והשאלות שממתינות לתשובה. בלי ווליום כל דפלוי מוחק את הידע שנצבר ומשאיר לקוחות שממתינים לתשובה שלא תגיע לעולם.

הנתיב היחסי `./data` הוא `/app/data` בקונטיינר, ולכן אין מה לשנות ב-`.env`:

```bash
railway volume add -m /app/data
railway redeploy
```

עלות: $0.15 ל-GB לחודש לפי שימוש בפועל — קובץ שיחות שוקל קילובייטים, כלומר סנטים בודדים.
לומר למשתמש את שתי ההשלכות: דפלוי עם ווליום כולל רגע קצר של downtime (Railway חוסם שני מופעים על אותו ווליום), ואי אפשר replicas.

### 8 · הפניית ה-webhook

```bash
npm run set-webhook -- https://<הכתובת>/webhook
```

**זו הנקודה שבה הבוט מתחיל לענות בוואטסאפ אמיתי.** לומר את זה למשתמש לפני ההרצה. ל-Green API לוקח עד 90 שניות להחיל.

### 9 · בדיקה מהטלפון

לבקש מהמשתמש לשלוח הודעה מהמספר שב-`ALLOWED_SENDERS`. אם אין תשובה — `railway logs`.

**להזכיר לו:** כשפותחים ללקוחות אמיתיים צריך לרוקן את `ALLOWED_SENDERS` **גם ב-Railway** ולא רק ב-`.env` המקומי:

```bash
railway variables --set "ALLOWED_SENDERS="
```
